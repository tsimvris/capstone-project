import styled from 'styled-components';

const StyledSignupLabel = styled.label`
	display: flex;
	flex-direction: column;
	align-items: center;
	height: 40px;
	margin-top: -20px;
	margin-bottom: 50px;
	color: var(--secondary);
	text-align: center;
	text-transform: uppercase;
`;

export default StyledSignupLabel;
