import styled from 'styled-components';

const StyledLabel = styled.label`
	height: 90px;
	color: var(--secondary);
	text-shadow: 8px 8px 15px #a7aaaf, -8px -8px 15px #fff;
	text-transform: uppercase;
`;

export default StyledLabel;
